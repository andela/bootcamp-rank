md-content(layout='column')
  section(layout='row', flex='')
    md-sidenav.md-sidenav-left.md-whiteframe-z2(md-component-id='left', md-is-locked-open="$mdMedia('gt-md')")
      md-list
        md-subheader.md-no-sticky
          | Bootcamp projects
          md-input-container
            i.material-icons  sort
            label Search
            input(ng-model='search')
        md-divider
        md-list-item.md-2-line(ng-repeat='project in projects | filter:search', ng-click='changeCurrentProject(project)')
          img.md-avatar.user-image(ng-src='{{project.picture}}', alt='{{message.who}}')
          .md-list-item-text(layout='column')
            h3 {{ project.project }}
            h4 {{ project.name }}
          md-divider
    md-content(flex='')
      md-card
        md-card-content
          md-subheader.md-no-sticky
            h2 Scoreboard
            md-divider
          md-subheader(ng-if='showRating')
            span.md-title {{currentProject.project}}
            br
            h5 {{currentProject.name}}
            p
              | {{currentProject.description}}
            md-button.md-raised.md-primary.md-padding(ng-click='github(currentProject.repository)')
              i.fa.fa-github.fa-1x
              |  View on github
            md-button.md-raised.md-primary.md-padding(ng-click='liveDemo(currentProject.demo)')
              i.fa.fa-globe.fa-1x
              |  Live demo
          md-subheader(ng-if='!showRating')
            | Select a project from the left panel to begin rating.
        br
      md-card.dash(ng-if='!showRating')
        md-card-content
          md-subheader.md-no-sticky
        br
      md-card
        md-card-content(ng-show='showRating')
          h2.md-title Add Rating
          h4
            small Rating Metric, provide honest opinion.
          md-divider
          .rating
            div(layout='column', layout-padding='')
              div(flex='2')
                i.fa.fa-certificate
                |  Project Quality:
              md-slider(layout-margin='', flex='5', md-discrete='', md-accent='', ng-model='rating.quality', step='1', min='1', max='10', aria-label='rating')
            div(layout='column', layout-padding='')
              div(flex='2')
                i.material-icons important_devices
                |  User Interface/Experience:
              md-slider(layout-margin='', flex='5', md-discrete='', md-warn='', ng-model='rating.uiux', step='1', min='1', max='10', aria-label='rating')
            div(layout='column', layout-padding='')
              div(flex='2')
                i.material-icons accessibility
                |  Confidence:
              md-slider(layout-margin='', flex='5', md-discrete='', md-hue='', ng-model='rating.confidence', step='1', min='1', max='10', aria-label='rating')
            div(layout='column', layout-padding='')
              div(flex='2')
                i.fa.fa-ship
                |  Project Understanding:
              md-slider(layout-margin='', flex='5', md-discrete='', md-primary='', ng-model='rating.understanding', step='1', min='1', max='10', aria-label='rating')
            md-input-container
              i.material-icons comment
              label Comment
              textarea(ng-model='rating.comment', columns='5')
            div(layout='row')
              span(flex='')
              md-button.md-raised.md-primary(ng-disabled='!user', ng-click='submitRating()') Submit
      div(flex='')
